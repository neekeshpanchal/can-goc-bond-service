version: "3.8"
services:
  pricing-api:
    build: .
    image: can-gov-bonds:local
    ports:
      - "5000:5000"
    environment:
      - APP_VERSION=local
    volumes:
      - ./app:/app   # useful for development; remove for strict production
